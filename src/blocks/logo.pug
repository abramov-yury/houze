mixin logo(parent)

  -
    const name = 'logo'
    className = getClassName(name, parent)

  a(class=className href="index.html")
    img.logo__img(src=(require('../../assets/img/logo.svg')) width="130" height="30" alt="Awesome logotype")

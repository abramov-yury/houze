mixin progress

  -
    stages = [
      {name: 'Основные данные', condition: 'checked'},
      {name: 'Удобства и условия', condition: 'active'},
      {name: 'Цена', condition: 0},
      {name: 'Тарифы', condition: 0},
      {name: 'Оплата', condition: 0},
    ]

    getItemName = (condition, name) => condition ? `${name} ${name}--${condition}` : name;

  ul.progress
    each stage, index in stages
      li(class=getItemName(stage.condition, 'progress__item') data-value=`stage-${index + 1}`) #{stage.name}
